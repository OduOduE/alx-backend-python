#!/usr/bin/env python3
''' Takes list of floats and returns their sum as floats '''
from typing import List


def sum_list(input_list: List[float]) -> float:
    ''' Takes list of floats and returns their sum as floats '''
    return float(sum(input_list))
